---
title: "Groq"
description: "Groq compound AI systems"
---

## Overview

AI Sonar provides access to Groq models. Total: **4 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `groq-compound` | Input $0.59 / Output $0.79 per 1M tokens |
| `groq-compound-mini` | Input $0.05 / Output $0.08 per 1M tokens |

## Speech-to-Text

| Model ID | Pricing |
|----------|--------|
| `whisper-large-v3` | $0.000030833333/second |
| `whisper-large-v3-turbo` | $0.000011111111/second |

## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.aisonar.dev/v1"
)

response = client.chat.completions.create(
    model="groq-compound",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
